1，切忌不要随便commit，如果你的代码出错，或者少了文件，你的权限可以随意覆盖现有的代码，别人update一下自己的代码就没了。
2，删除文件不要直接删除，用svn的delete功能
3，重命名和上述一样，用svn功能rename
4，svn只管理源代码，不管理编译好的dll，所以切忌不要将项目目录中的obj目录和bin目录包括进来，让这2个目录变成不受svn管制的目录（即unversiond file，图标显示为一个？符号）
5，切忌以上4条，你所有的commit动作，都将影响整个项目的正确与否，所以切勿胡来！

by 沈  毅 2010-8-4